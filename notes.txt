CREATE TABLE userbase (
   id INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
   fullname VARCHAR ( 50 ),
   username VARCHAR ( 50 ),
   membership VARCHAR ( 5 )
);

INSERT INTO userbase (fullname, username, membership)
VALUES 
  ('user1', 'user1@email.com', 'no'),
  ('user2', 'user2@email.com', 'no');

ALTER TABLE userbase
ADD password VARCHAR ( 255 );

UPDATE userbase
SET membership = 'yes'
WHERE username = ${username};

CREATE TABLE testing (
   id INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
   mytext VARCHAR ( 50 ),
   mytimestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
);

INSERT INTO testing (mytext)
VALUES 
  ('hewwo');

CREATE TABLE messages (
   id INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
   username VARCHAR ( 50 ),
   title VARCHAR ( 50 ),
   text VARCHAR ( 250 ),
   timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO messages (username, title, text)
VALUES 
  ('jon@email.com', 'TITLE ONE', 'SALUTATIONS'),
  ('jon@email.com', 'TITLE TWO', 'I BE MESSAGING');